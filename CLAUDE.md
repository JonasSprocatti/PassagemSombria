# Passagem Sombria — Deck de Campo Online

Mesa virtual (VTT) para o RPG espacial *Passagem Sombria*. SPA estática em **JavaScript puro (ES modules)**, sem build. Backend: **Supabase** (Auth + Postgres + Realtime + Storage). Hospedagem: Vercel (site estático, raiz = `public/`). PT-BR em toda a UI, comentários e nomes.

## Como rodar

Precisa ser servido por HTTP (usa ES modules nativos — `file://` não funciona).

```
cd public
python -m http.server 3000   # abre http://localhost:3000
```

Antes: preencher `public/js/config.js` com `SUPABASE_URL` e `SUPABASE_ANON_KEY` (não versionado / placeholders). Segurança real fica na RLS do Supabase, não no front.

Não há lint. Há um início de testes automatizados em `tests/` (ver seção **Testes automatizados** mais abaixo) cobrindo as funções puras de `dados-jogo.js` — o resto da verificação ainda é manual no navegador (com um projeto Supabase configurado).

## Estrutura de arquivos

| Arquivo | Papel |
|---|---|
| `public/index.html` | Shell mínimo: monta `#app`, registra o service worker, carrega `js/app.js` como módulo. |
| `public/js/app.js` | **~4270 linhas — o app inteiro.** Roteador por hash, todas as telas, combate, chat, bancada, painel admin. Ponto de entrada. |
| `public/js/config.js` | Credenciais Supabase (placeholders no repo). |
| `public/js/dados-jogo.js` | Dados do sistema: `RACAS`, `CLASSES`, `FILOSOFIAS`, `ARMAS`, `ARMADURAS`, `IMPLANTES`, `SCRIPTS`, `NAVES`, `PERICIAS`, `KEYWORDS`/`PALAVRAS_CHAVE`, `CONSUMIVEIS`, pentes (`TIROS_POR_PENTE=3`, `PENTES_MAX=4`, `TIPOS_PENTE`), `MODS_ARMA`/`SLOTS_ARMA` (bancada), `UPGRADES_NAVE`, `AVARIAS`, `ESTACOES`. Helpers puros: `propsArma`, `chavesDaArma`, `custoTiro`, `modsDoSlot`, `acharMod`. |
| `public/js/efeitos.js` | Motor de **efeitos declarados** de fichas: `FichaEfeitos`, `Portador`, `EFEITOS`, `MOMENTOS`, `CONDICOES_ALVO`, `validarEfeitos`. Permite que conteúdo novo (implante/raça/classe/arma) altere atributos sem novo código. Zero imports — importável direto no Node. |
| `public/js/regras.js` | **Regras puras** (ficha, dados, condições, campo tático, tipos de dano): `calc`, `novaFichaDados`, `dcSalvaguarda`, `ganhosDoNivel`, `CONTEUDO_EXTRA`, `modosAtivosDe`, `aplicarCond`/`CONDICOES_INFO`/`infoCond`, `distCombate`/`posInicial`/`empurrarDe`/constantes do campo tático, `tipoDanoArma`/`tipoDanoAtaque`/`imuneAoDano`/`alcanceDaArma`, `danoCritico`, `d`/`sign`/`rollNd`/`parseDice`. Extraído de `app.js` só pra ficar testável no Node sem simular navegador nem Supabase — ver **Testes automatizados**. `app.js` importa tudo isso de volta; nada mudou em comportamento. |
| `public/js/biblioteca.js` | **`telaBiblioteca(aba)`** — a biblioteca inteira (raças, classes, arsenal, bestiário, NPCs, a aba Regras…), extraída de `app.js` pra reduzir o monólito. Import dinâmico no roteador (`case "biblioteca"`), no mesmo padrão de `mapa-sistema.js`/`sistema-solar.js`/`criacao.js`. Importa de volta de `app.js` os helpers que `telaFicha`/`telaMesa` também usam (`shell`, `esc`, `img`, `imgFig`, `extras`, `escalaTabela`, `etiquetasKw`, `todasArmas`/`todasArmaduras`/`todosImplantes`/`todasNaves`/`todasCriaturas`/`todosNPCs`/`todosConsumiveis`, `perfil` — todos `export`ados em `app.js` especificamente pra isso) e o resto direto de `dados-jogo.js`/`dados-bestiario.js`/`dados-npcs.js`/`regras.js`. O botão "🛠 Administrar conteúdo" faz `import("./admin.js")` na hora do clique — a Biblioteca nunca carrega o painel de admin sozinha. **Busca** (`#bib-busca`): filtra os cards (`details`/`div.det`, filhos diretos de `section.sec`) da aba atual pelo `textContent`, sem re-renderizar nada — só `el.hidden`. `indiceAba(id)` é um índice leve (só nomes, recalculado a cada tecla a partir das mesmas listas do render) usado só quando a aba atual não tem resultado, pra avisar em quais outras abas o termo aparece. `buscaAtual` é `let` de módulo — sobrevive a trocar de aba (inclusive pelo link do índice cruzado), mas não a um F5. |
| `public/js/admin.js` | **`painelAdmin(voltarPara)`** — CRUD do conteúdo global (tabela `conteudo`): criaturas, armas, armaduras, implantes, consumíveis, naves, NPCs, palavras-chave, raças, classes, mecânicas, imagens vinculadas, e a visão somente-leitura do roster de fichas. Extraído de `app.js` (era a última função do arquivo, ~690 linhas). Import dinâmico a partir de `biblioteca.js`; ao fechar, importa `biblioteca.js` de volta pra re-renderizar (precisa mesmo re-render, não só trocar o hash — o conteúdo editado pode ter mudado). Importa de `app.js`: `sb`, `esc`, `usuario`, `criaturaMod`, `conteudoMod`, `sincronizarExtra`, `gerarFichaHTML`, `imprimirFichaHTML` (os quatro últimos ganharam `export` especificamente pra isso). **Bug real corrigido na extração**: o editor genérico de item (`editorItem`) checava `item.efeitos` num `oninput` do campo Nome — `item` nunca existia nesse escopo (o nome certo é `it`), então digitar o nome de qualquer arma/armadura/implante/NPC/etc. novo ou editado lançava `ReferenceError` em silêncio. Corrigido pra `it.efeitos` ao mover a função.
**Campos que eram texto livre e viraram seleção** (o texto livre deixava passar typo que fazia a mecânica ficar inerte, sem nenhum aviso): **Tipo de dano** do efeito "dano" de criatura → `<select>` com `TIPOS_DANO` (regras.js). **"Imune a"** (efeito `imunidade`, nos dois editores) → `<select data-ef-imune>`/`<select data-c="a">` com `TIPOS_DANO` + `CONDICOES` + opção **"Outro (descrever)…"** (sentinela `OUTRO_IMUNE = "__outro__"`, constante de módulo em `admin.js`) que revela um campo de texto pra imunidade narrativa (surpresa, terreno difícil…) — gerado por `opcoesImuneA(valorAtual)`. **"Anula"** do efeito `zona` (aura) → checkboxes de `OPCOES_NEGA` (as 3 únicas strings que `auraNega()` reconhece: tecnomancia/cura/implantes) em vez de texto separado por vírgula. **Validação da sentinela**: `criaturas.js` `validar()` e `efeitos.js` `validarEfeitos()` rejeitam `imunidade` com `a` vazio ou `"__outro__"` — trava o botão Salvar até o admin realmente escolher ou descrever algo (couraça só-com-`limiar`, sem `a`, continua válida — é o caso da Couraça de Anéis). |
| `public/js/criaturas.js` | Motor de efeitos automáticos de **criaturas/inimigos** em combate: `class Criatura`, `GATILHOS`, `EFEITOS`, `criar`, `validar`. Carregado sob demanda. |
| `public/js/ui.js` | Modais reutilizáveis: `modalForm`, `confirmModal` (substituem `prompt`/`confirm`). Sons (`somDado`, `somCritico`…), `notificar`, prefs de som. Overlay `.mdl-overlay`. |
| `public/js/conteudo.js` | Conteúdo global editável pelo painel admin (tabela `conteudo`): `carregarConteudo`, `conteudo()`, `comAjustes`, `imagemDe`, `thumb`, `figura`. Cache em memória. |
| `public/js/dados-bestiario.js` | `BESTIARIO`, `NIVEIS_AMEACA`. |
| `public/js/dados-npcs.js` | `NPCS`, `PAPEIS`. |
| `public/js/dados-mestre.js` | `FACCOES`, `NIVEIS_REPUTACAO`, `TABELAS` (rolagem aleatória do Mestre), `REFERENCIA`. |
| `public/js/mapa-sistema.js` | **VTT ativo** (mapa compartilhado do Mestre): `abrirMapa({ mapa, combate, souMestre, salvar, aoFechar })`, `POI_TIPOS`. Import dinâmico. |
| `public/js/sistema-solar.js` | Seletor 3D de planeta/raça (Three.js sob demanda): `abrirSeletorPlanetas(RACAS, onSelect)`. Import dinâmico. |
| `public/js/criacao.js` | Assistente de criação de personagem: `abrirCriacao({ onCriar, onCancelar, abrirSistemaSolar })`. Import dinâmico. |
| `public/sw.js` | Service worker PWA. `CACHE = "ps-shell-vNN"` — **incrementar a cada deploy relevante**. HTML: network-first; estáticos: stale-while-revalidate; Supabase/esm.sh: rede direta. |
| `public/css/estilo.css` | Todo o CSS. Seções marcadas com `/* ---- NOME ---- */`. |
| `public/js/mapa.js` | **LEGADO / não importado.** Substituído por `mapa-sistema.js`. Não editar sem confirmar.

Bibliotecas via CDN direto (sem npm): `@supabase/supabase-js@2` de `esm.sh`, Three.js sob demanda. Fontes: Google Fonts (Chakra Petch, IBM Plex Sans/Mono).

## Mapa interno de `app.js`

Seções demarcadas por comentários `// ---------------- NOME ----------------`. Faixas aproximadas (linhas deslocam com edições):

| Linhas | Bloco |
|---|---|
| 1–95 | Imports, `sb` (client Supabase exportado), helpers de dados (`parseDice`, `rollNd`, `d`, `sign`), `sessaoAtiva`. |
| — | **Ficha**: `novaFichaDados()` (shape do objeto `f`), `calc(f)` → `k` (derivados: `attr`, `per`, `ramMax/ramLivre`, `cd`, `conj`, `iniciativa`, `deslocamento`, `k.efeitos`), `ganhosDoNivel` — hoje moram em `public/js/regras.js`, importados no topo de `app.js`. |
| 192–318 | Ficha imprimível (`gerarFichaHTML`, imprimir/baixar). |
| 319–440 | Descansos (curto/longo), `habilidadesAtivas`, `reporPentes`, `aplicarDescanso`, `danoArma`. |
| 441–485 | Combate espacial + ferramentas do Mestre (`danoNave`, `orcamentoEncontro`, `sugerirEncontro`, `rolarTabela`). |
| 486–570 | `CONDICOES_INFO`/`CONDICOES`, temas, acessibilidade (`aplicarA11y`, `montarA11y`). |
| 571–640 | **Roteador** (`rotear` — hash routes), `telaFichaPublica`, `shell()` (layout base). |
| 642–775 | `CONTEUDO_EXTRA` (espelho síncrono do conteúdo admin p/ `calc`), `armaMontada` (aplica mods à arma), **`abrirBancada`** (HUD de mods de arma). |
| 776–860 | `etiquetasKw`, `escalaTabela`, lazy `criaturaMod`/`conteudoMod`, `sincronizarExtra`. |
| 860–935 | `avisar` (toast com desfazer), atalhos de teclado. |
| 935–1030 | `estadoArma`, `normalizaPentes`, animações (`contarAte`, `cenaNivel`, `animarBarras`), `iniciar()` (bootstrap). |
| 1046–1120 | `telaLogin`, `telaHangar` (lista de personagens). |
| 1122–1646 | **`telaFicha(id)`** — edição completa, level up, loja, inventário, equipar, bancada, nano-tatuagens. |
| 1647–1682 | `telaCampanhas` (criar / entrar por código). |
| 1683–3614 | **`telaMesa(id)`** — a maior. Chat + rolagens + dano/cura + combate pessoal + combate espacial + nave + postos + mapa + diário + estatísticas. Realtime via `sb.channel('mesa-<id>')`. Sub-blocos: painel de combate (~1790), aba "Meu personagem" (~1846), munição/pentes (~1849), ações de mesa e **handler `[data-atq]`** de ataque de arma (~2967), habilidades ativas (~3168), scripts/conjuração (~3390), postos de nave (~3410), estaleiro/upgrades (~3490). |
| — | `telaBiblioteca` (raças, classes, armas… somente leitura) + a aba **Regras** (`aba === "regras"`) — o manual de bolso do jogador, **gerado a partir das próprias constantes** (`CONDICOES_INFO`, `TIPOS_DANO`, `ALCANCE_*`, `CAMPO_LARGURA`, `TIROS_POR_PENTE`, `PERICIAS`). Regra que muda no código muda o texto junto; nunca escrever número à mão ali se existir constante. Atalho `📖 REGRAS` no topo da mesa abre em aba nova (`target="_blank"`), pra não fechar a sessão. **Mora em `public/js/biblioteca.js` agora**, não em `app.js` — ver a linha da tabela de arquivos acima. |
| 3703–4270 | **`painelAdmin`** — CRUD do conteúdo global (tabela `conteudo`), ajustes e imagens. |

## Modelo de dados

- **`f`** = objeto `dados` do personagem (JSONB em `personagens.dados`). Shape canônico em `novaFichaDados()`. Sempre mesclar sobre ele: `{ ...novaFichaDados(), ...(row.dados||{}) }`.
- **`k = calc(f)`** = derivados recalculados a cada render; nunca persistido. `k.efeitos` é a instância `FichaEfeitos` (motor declarativo).
- **`dcSalvaguarda(k, atributo)`** = `8 + mod` — CD de teste de resistência forçado por **habilidade** de personagem (`salvaguarda`). Palavras-chave de arma e granadas usam o `cd` **fixo do texto** (13 físico / 14 Acrobacia); granadas com `pericia` (Acrobacia) rolam `d20 + attr + perícia`.
- **`aplicarCond(alvo, cond, turnos)`** (módulo) — aplica condição no rastreador com a regra da mesa: **dano contínuo** (Sangrando/Em chamas/Envenenado) empilha **+1 turno** se já ativa (nunca aumenta o dado, sempre 1d4/1d6/1d4 fixo); **estado** renova para a maior duração. Guarda `turnos+1` para condições de estado porque o `#cb-prox` decrementa no início do turno do afetado antes dele agir. Padrão de duração: hard (Atordoado/Paralisado/Congelado) 1t · estado (Cego/Caído/Marcado/Lento/Amedrontado/Enfraquecido/Silenciado) 2t · dano 3t.
- **Condições no fluxo de ataque** (`[data-atq]`): atacante **Cego/Amedrontado/Envenenado** → Desvantagem; **Enfraquecido** → `danoFinal × 0.5`; **Atordoado/Paralisado** (não-Mestre) → bloqueio via `minhaTrava()`. Alvo **Marcado** → +2 acerto de quem o ataca; alvo **Atordoado/Paralisado/Caído/Cego** → Vantagem contra ele. Vant/desv de todas as fontes somam e reduzem a −1/0/+1. `#conjurar` e `[data-hab-usar]` também respeitam `minhaTrava(["silenciado"])`. **Paralisado a até 2m** é Crítico automático (não só Vantagem) — `paralisadoPerto`/`multCrit` em `[data-atq]` e `paralisadoPerto2`/`critAuto2` em `.cb-atk`; nat 1 ainda falha. Já foi bug real: só dava Vantagem, igual às outras condições do grupo `alvoAberto`, sem o multiplicador que o texto da condição promete.
- **Movimento no campo tático × condições**: Paralisado/Congelado/**Lento** bloqueiam o movimento por completo (deslocamento zero); só **Caído** (ou ter tentado **Apagar fogo** no próprio turno, `c._movMetade === rodada`) reduz a deslocamento à metade. Já foi bug real: Lento estava agrupado com Caído (metade do deslocamento) em vez de "perde a Ação de Movimento" (zero) — checar em `.cb-token.movivel` → `pointerdown`.
- **Mecânicas de condição que exigiam ação do jogador** (`.acoes-mesa`, aba "Meu personagem", só aparecem em combate ativo e com a condição presente):
  - **🔥 Apagar fogo** (`#cb-apagar-fogo`): gasta Ação Principal **e** Reação (duas chamadas a `gastarAcao`), marca `linha._movMetade = rodada` (deslocamento pela metade neste turno, limpo no `#cb-prox` seguinte), rola **d20 puro sem modificador** — ≥10 remove "Em chamas" na hora, <10 continua queimando (mas as ações já foram gastas).
  - **🧎 Levantar** (`#cb-levantar`): gasta Ação Principal **e** Ação de Movimento, remove "Caído" na hora, sem rolagem.
  - Os dois respeitam o mesmo gate de turno do `[data-atq]` (não-Mestre só usa no próprio turno) e `minhaTrava()`.
- **Desvantagem de condição em testes de perícia** (fora de ataque): `desvPorTeste(condsList, pericia)` em `telaMesa` — **Cego** só atrapalha testes visuais (`PERICIAS_VISUAIS`: Percepção, Investigação, Prestidigitação, Pilotagem); **Envenenado** só testes físicos (`PERICIAS_FISICAS`: Atletismo, Acrobacia, Furtividade). Usado em `#rolar-per` e nos dois lados do `#teste-oposto` (`rolarEEnviar` ganhou um 4º parâmetro `vantOverride` pra isso). Fora dessas listas, a condição não pesa no teste — é intencional (não é geral, só onde faz sentido narrativo).
- **Amedrontado com fonte rastreada**: `aplicarCond(alvo, cond, turnos, origemId)` — 4º parâmetro opcional, guardado como `cond.origemId` na linha do rastreador. Preenchido pelas 3 rotas que hoje aplicam Amedrontado no atacante real: `aplicarEmAlvos` (salvaguardas/scripts/granadas, usa `minhaLinhaCb()?.id`), o gatilho `ao_acertar` de criatura em `.cb-atk` (usa `c.id`), e `aplicarCondsNoAlvo` de palavra-chave/munição em `[data-atq]` (usa `minhaLinhaCb()?.id`). Com `origemId` conhecido: a Desvantagem em `[data-atq]` só vale atacando essa fonte (`desvPorMedo`), e o bloqueio de aproximação no campo tático (`.cb-token.movivel`) mede a distância só até ela, não até "qualquer inimigo". Sem `origemId` (condição aplicada por uma rota antiga que não foi atualizada, ex. o Mestre aplicando manualmente por `.cb-cond-add`) cai no comportamento antigo — vale contra/para qualquer inimigo, pra não enfraquecer a condição silenciosamente.
- **`aplicarDanoAlvo(alvo, valor)`** (helper em `telaMesa`) = ponto único de todo dano ROLADO contra um combatente. Ordem: couraça por limiar de criatura (`habs` com `imunidade.limiar`) → redução de dano do alvo (`k.efeitos.aoSofrer().reducao`, jogador) → corpo (`sincronizarFicha`). Nave → `danoNave` + persiste `camp.nave` se `nave_party`. Os botões `±5` / definir HP do Mestre NÃO passam por aqui (ajuste livre). Usado por `.m-aplicar`, blocos `alvoNave`/`alvoCombatente` do `[data-atq]` e `aplicarEmAlvos`.
- **`c.cd` do jogador no rastreador é uma FOTO**, tirada uma vez em `#cb-iniciar` (`cd: kk.cd`) — não recalcula sozinha depois. `sincronizarCdCombate(personagemId, dadosFicha)` (helper em `telaMesa`, perto de `salvarCombate`) recomputa `calc(dados).cd` e atualiza a linha se mudou; chamado nos 4 pontos onde `f.modos`/`f.modosAte` mudam em combate: ativar modo/criar (Criogénese, Endurecer…), Êxtase da Batalha (`tipo:"tabela"`), ligar/desligar o gás do Ven'y, e a expiração automática de modos no `#cb-prox`. Sem isso, um jogador que ativa Criogénese (Escudo de gelo, +2 Defesa) **em pleno combate** segue tomando dano como se a CD não tivesse mudado, porque `.cb-atk`/`[data-atq]`/`aplicarEmAlvos` checam `alvo.cd` da linha, não recalculam do zero. Já foi bug real — CD do Urak ficou 2 pontos abaixo do esperado depois de criar o escudo em combate.
- **`defesaNaveParty()`** = `10 + manobra + bonusDefVeiculo()`, onde `bonusDefVeiculo()` soma `efeitos.defesaVeiculo()` do personagem do piloto no leme (Instinto Evasivo). Usado no painel de combate, aba nave e Manobra Evasiva.
- **Implantes inertes (Zona Morta)**: `calc(f, { semImplantes: true })` recalcula a ficha inteira como se a pessoa não tivesse implante nenhum — some o +2 de RAM do Chip, o +1 de CD das Placas, a carga de Cinético e todo efeito declarado de implante; marca `k.implantesInertes`. `telaMesa.render()` passa `semImplantes: auraNega("implantes")` (a própria pessoa) e `aplicarDanoAlvo` passa `auraNega("implantes", alvo)` (para a redução de dano do alvo também cair). Fora de combate, ou fora do raio, nada muda.
- **`aurasAtivas()` / `auraNega(oque)`** = auras de criatura (`hab.gatilho === "aura"`) das criaturas vivas no rastreador. Sem posição, valem para todo o combate. `auraNega("tecnomancia")` bloqueia `#conjurar`; `auraNega("cura")` bloqueia `#enviar-cura`. Banner no painel de combate lista todas. **`#cb-add-btn` copia `b.habs` para a linha do combate** — sem isso, auras, couraça por limiar (`aplicarDanoAlvo`) e efeitos automáticos de criatura (`#cb-prox`) não funcionavam para bicho do bestiário. Criaturas com aura: `Silenciador Cósmico` (nega tecnomancia+implantes), `A Ferida do Mundo` (nega cura+tecnomancia+implantes).
- **`modosAtivosDe(f)`** lê efeitos de modo em `h.opcoes`/`h.resolve.opcoes` (gás Ven'y, Êxtase) **e** em `h.resolve.efeitos` (Endurecer — modo sem opções). Sem esse último, Endurecer não reduzia dano.
- **Ataque de jogador em combate**: não-Mestre só ataca no próprio turno (bloqueio; o Mestre rola pelo ⚔ da linha dele). Com alvo escolhido, o seletor abre já com um inimigo selecionado; "sem alvo" é a última opção (para atacar algo fora do rastreador).
- **Modo Jogador** (`#modo-jogador`, só para `ehMestreReal`): toggle no topo da mesa que zera `ui.souMestre` (guardado em `localStorage` `ps-modojog-<id>`) para o Mestre testar/jogar como tripulante — some as ferramentas de Mestre e valem os bloqueios (turno, Atordoado). `ehMestreReal` guarda a permissão verdadeira.
- **Objeto `ui` em `telaMesa`** — Passo 0 da modularização (ver plano). `souMestre`, `modoJogador`, `meuPers`, `tokenSel`, `campoZoom`, `pintarMsg`, `mapaCtrl`, `abaMesa`, `timerInt`, `vantagem`, `privada`, `asCegas`, `recapFeita`, `gravandoAte` eram `let`s soltos na closure; agora moram em `const ui = {...}`, criado **uma vez** por chamada de `telaMesa(id)` (não a cada `render()`) — mesma referência sempre, só os campos dentro mudam. Motivo: assim que `telaMesa` for dividida em arquivos (`mesa-mestre.js`/`mesa-nave.js`/`mesa-combate.js`/`mesa-ficha.js`, ainda não feito), closures não atravessam módulos — estado mutável compartilhado entre "abas" precisa viver numa referência estável (`ctx.ui`) em vez de variável solta. Este passo não mudou nenhum comportamento visível, só onde cada variável mora — qualquer leitura/escrita que antes era `meuPers`/`souMestre`/etc. agora é `ui.meuPers`/`ui.souMestre`/etc.
- **`.cb-atk`** (⚔ na linha do rastreador, Mestre): agora **mira um alvo primeiro** e resolve acerto (`acerto` vs `alvo.cd`, +2 se alvo Marcado, Vantagem se alvo exposto) + dano via `aplicarDanoAlvo`, marcando `alvo_resolvido`. Ataques `bonus == null` (efeito automático / teste de resistência) continuam só postando o dado.
- **Campo tático** (`#cb-campo` no painel de Combate): grade 2D rasa — `CAMPO_LARGURA=40`m, `CAMPO_PISTAS=3` (frente/meio/fundo), `PISTA_M=1`m entre pistas (profundidade rasa), `ALCANCE_CAC=1.5`m (corpo-a-corpo), `ALCANCE_ARMA={curto:15,medio:30,longo:40}`. Cada combatente não-nave tem `c.pos = {x, lane}`. `distCombate(a,b)` = `hypot(dx, dlane*PISTA_M)`. `posInicial(ordem, tipo)` — aliados à esquerda / inimigos à direita, espalhados nas pistas; atribuído em `#cb-add-btn`, no auto-invocar e migrado em massa no início de `telaMesa`. **Janela visível**: `vistaCampo {ini,fim}` + `pctX(x)` calculados por render — campo inteiro (40 m) ou aproximado (`campoZoom`, janela `CAMPO_JANELA=12` m centrada no token selecionado / no turno atual). A grade vem de `--g1`/`--g5` inline (passo em % da janela), então o `repeating-linear-gradient` do CSS serve as duas escalas. Régua adapta o passo (1 m no zoom, 5 m no geral). **Seleção** (`tokenSel`): clique num token desenha a banda `.cb-alcance` (±`ALCANCE_CAC`), marca quem está no alcance (`.perto`, prefixo ⚔) vs fora (`.longe`, esmaecido) e lista distâncias em `.cb-distancias`. Arrastar (`.cb-token.movivel`): jogador move o seu só no turno, limite `k.deslocamento` (½ só Caído; 0 Paralisado/Congelado/**Lento**); Mestre move qualquer um; clique sem arrastar (<0,4 m) seleciona. O arraste converte px→metros pela janela visível, não pela largura total. **Alcance de arma ligado**: `alcanceDaArma(cat, pr)` → branca `ALCANCE_CAC` (1,5 m; 3 m com a chave Alcance), fogo `ALCANCE_ARMA.curto/medio/longo` (15/30/40) conforme Alcance Maior / Telescópica. No `[data-atq]` a checagem roda **antes de gastar munição**: jogador sem ninguém no alcance leva `alert` e o tiro nem acontece; o seletor só lista quem está no alcance (o Mestre vê todos, marcados com “⚠ fora de alcance”, e pode forçar — aí a resolução posta “não alcança” sem dano). `.cb-atk` mostra a distância de cada alvo e avisa fora de alcance (sem bloquear — quem conduz é o Mestre); ataques derivados de jogador carregam `alcance`. **Área por ponto de impacto**: já ligado — `aplicarEmAlvos({area:true, raio})` deixa escolher o epicentro entre quem tem `pos` e marca automaticamente todo mundo dentro do raio (`distCombate(epi, x) ≤ raio`), aliado incluído. **Amedrontado não se aproxima**: já ligado, com fonte rastreada por `origemId` (ver Modelo de dados). **Auras por raio**: já ligado — `aurasSobre(quem, oque)` filtra por `distCombate(a.fonte, quem) <= a.raio`; sem raio declarado ou sem posição dos dois lados, vale pro combate inteiro (comportamento antigo). **Empurrão**: `empurrarDe(origem, alvo, metros)` (regras.js, pura) devolve a nova posição jogando o alvo pra longe no eixo X, com clamp no campo; `aplicarEmAlvos({empurrao})` aplica de verdade no token.
- **Gatilhos de criatura** (`#cb-prox`): `INICIO_TURNO` agora resolve `invocar` (auto-adiciona a criatura ao rastreador com `pos`) e `condicao` (`alvo:"proprio"` aplica no próprio; senão narra); `FIM_TURNO` roda para quem encerrou o turno; varredura `AO_MORRER` uma vez por turno em quem caiu (marca `_morreu`). `zona`/`imunidade` fora de `gatilho:"aura"` são narrados.
- **`habsDoCombatente(c)`** — linhas antigas do rastreador foram salvas **sem `habs`**, o que matava auras, regeneração, invocação e couraça. Reidrata do bestiário pelo nome (tirando o sufixo ` #N`) e gruda na linha. Usado por `aurasAtivas`, `aplicarDanoAlvo` (limiar) e os três gatilhos do `#cb-prox`. Sempre preferir a ele em vez de ler `c.habs` direto.
- **`aplicarEmAlvos` retorna** `true` (resolveu ou narrou) / `false` (cancelou sem mirar). Chamadores devolvem o recurso no `false`: `#conjurar` (RAM), `.item-usa` (granada), `[data-hab-usar]` salvaguarda (não marca `usos`).
- **Condições no rastreador**: `.cb-conds` saiu de dentro de `.cb-nome` (que tem `nowrap`+`overflow:hidden` e escondia múltiplas) — agora é um bloco `flex:1 1 100%` que quebra para uma faixa própria sob a linha. Badge azul (`estado`) para condição sem dano, vermelho (`sangra`) para com dano.
- **Munição**: cada arma de fogo tem o próprio pente carregado (`item.tiros`, `item.tipoPente` no inventário); a reserva (`f.pentes`, mapa `tipo → qtd`) é compartilhada. Migração de fichas antigas via flag `__migrouArma` / `f.tirosPente` (legado de pente único).
- **Limite de pentes**: `k.pentesMax = max(2, 5 + mod de Força)` (1 no cano + resto na reserva); `k.pentesReserva = pentesMax − 1` é o teto da mochila. Calculado em `calc()` após os efeitos (buffs de Força contam). Sites sem `k` usam `pentesReservaDe(f)` (chama `calc`). `dados-jogo.js` ainda exporta o antigo `PENTES_MAX=4`, mas o app não usa mais para o teto.
- **`propsArma(cat).efeito`** = descrição legível das palavras-chave da arma, montada palavra por palavra (`chaves.map(c => KEYWORDS[c.nome])`). Já foi bug real: buscava `KEYWORDS` pela string **inteira e crua** da arma (`"Destruidora, Perfurante, Aparar"`), que nunca bate com nenhuma chave do dicionário (que é uma por palavra-chave), e caía de volta no próprio `cat.kw` — a mensagem de ataque saía com `🏷 Destruidora, Perfurante, Aparar: Destruidora, Perfurante, Aparar.` (rótulo repetindo o valor). Corrigido; e a mensagem de ataque (`[data-atq]`) parou de reexibir as palavras-chave ali (já aparecem nos marcadores — Brutal, Ágil — e em "ignora N de armadura"), pra não duplicar informação.
- **Revisão completa de `PALAVRAS_CHAVE`** — passou de ~45 entradas (várias mortas, duplicadas ou compostas) para ~38 entradas **atômicas e composíveis**: armas agora podem ter mais de uma palavra-chave separada por vírgula (`chavesDaArma` já suportava isso desde sempre — só ninguém usava). Achados e correções:
  - **`contra:"robos"` nunca funcionava**: `Anti-Sintético`/`Ferramenta` (+2 dano contra robôs) dependem de `FichaEfeitos.modificarAtaque({..., alvo})`'s `combina("robos")`, que lê `alvo?.nome`/`alvo?.categoria` — mas nenhum dos 3 pontos que chamam `modificarAtaque` em `[data-atq]` passava `alvo` nenhum. O efeito nunca aplicava, em ataque nenhum, desde sempre. Corrigido passando `alvo: alvoParaEfeitos` (o combatente/nave já mirado) nas 3 chamadas.
  - **4 palavras-chave 100% mortas** (`{props:{}}`, nada acontecia apesar do texto prometer): `Confiável` → agora nenhum dado de dano rola abaixo da metade da face, arredondado pra cima (`pr.confiavel`, aplicado logo após rolar `dados` em `[data-atq]`). `Pesada` → −2 no acerto de verdade (`pr.pesada`), negado se `k.efeitos.imunidades()` contiver "penalidade de -2 com armas Pesadas" — a passiva **Memória Muscular** do Soldado já dizia anular essa penalidade, mas nada a aplicava; achado ao mecanizar isto. `Sobreaquecimento` → num natural 1, 1d4 de dano térmico em quem atira (`pr.sobreaquece`). `Inesquivável` → ignora cobertura (não dá pra se esconder de um tiro assim).
  - **Um mod de bancada 100% morto, achado na mesma revisão**: "Núcleo Térmico" (`MODS_ARMA`) declarava `kw:"Em chamas"` — essa palavra-chave nunca existiu (só a CONDIÇÃO se chama assim; a palavra-chave que a aplica sempre foi outra). Corrigido pra `kw:"Queimadura"`.
  - **Compostos decompostos em peças atômicas, compostas via vírgula** (em vez de nomes congelados numa combinação só): `Pesada / Queimadura` → `Pesada, Queimadura`. `Pesada / Fogo de Supressão` → `Pesada, Fogo de Supressão`. `Silenciosa / Toxina Lenta` → `Silenciosa, Tóxica` (a "Toxina Lenta" em si era só Oculta+Tóxica juntas — removida). `Rajada Silenciosa` → `Rajada, Silenciosa`. `Compacto e Ágil` → `Ágil, Oculta`. `Curto Alcance / Descarregar` → `Curto Alcance, Descarregar`. `Despedaçador` (Brutal + ignora 1 de armadura) → `Brutal, Perfurante Leve`. `Destruidora` (Brutal + Vantagem no acerto) → `Brutal, Certeira` (nova palavra-chave: só a Vantagem no acerto, reutilizável em qualquer combinação futura). `Sangramento em Área` → `Área, Sangramento`. `Inesquivável / Contínuo` → `Inesquivável, Sangramento` (o "Contínuo" era literalmente o mesmo efeito de `Sangramento`: 3 turnos, sem CD). Exceção deliberada: `Marcador Térmico` (Alcance + aplica Marcado) NÃO virou `Alcance, Marcador` — a palavra-chave `Alcance` tem texto "3m corpo a corpo" pensado pra armas brancas, ficaria errado numa arma de fogo; virou só `Marcador`, self-contido com seu próprio `props.alcance:true`. `Ultra-Oculta / Surpresa` também ficou intacta de propósito — não é duas coisas nomeáveis separadamente, é uma habilidade só com um bônus de acerto intrínseco ao conceito.
  - **Aliases redundantes apagados** (mesmo efeito, nome alternativo, zero motivo pra ter os dois): `Mira Telescópica` → usar `Telescópica`. `Área 3x3m` e `Explosão em Área 3x3` → usar `Área`.
  - **`raio`/`empurrao`/`ignoraArmadura` SOMAM entre fontes, não pegam só a maior nem sobrescrevem** (`propsArma()`, `dados-jogo.js`): uma arma com duas palavras-chave/mods que concedem o MESMO tipo de bônus (ex. `Perfurante` + `Perfurante Leve` → ignora 3, não 2) empilha os dois valores. Decisão explícita do usuário depois de eu ter implementado `Math.max` por padrão (mais conservador) — perguntei e a resposta foi somar. Vale também pra combinações futuras com mods de bancada, não só entre palavras-chave da própria arma.
  - **Área com raio de verdade** (`Área`, `Rajada`, `Cone de Repulsão`, `Artilharia`, `Atravessa Paredes`): cada uma tem `props.raio` (metros). Antes a "Área" no ataque de jogador (`[data-atq]`) era só um texto "◎ Área: ..." — o ataque sempre resolvia contra 1 alvo só. Agora, com `pr.area && pr.raio` e combate ativo, `[data-atq]` abre um seletor de **epicentro** entre quem tem posição no campo tático (mesmo padrão do `aplicarEmAlvos` de scripts/granadas do Mestre — `distCombate(epi, x) <= raio` decide quem é pego, aliado incluído) em vez do seletor de alvo único; sem ninguém posicionado, cai de volta no fluxo de alvo único de sempre (fallback seguro). O alvo "principal" (de preferência inimigo) segue o MESMO caminho de sempre (crítico, furtivo, condições, mensagem); os demais pegos pelo raio são resolvidos num laço à parte logo depois — MESMO dano já rolado (é a mesma rajada/explosão), UM d20+mod fresco por alvo contra a Defesa de cada um (sem vantagem/desvantagem, mesma simplificação que os scripts de área do Mestre já usam). `Cone de Repulsão` (`props.empurrao`) também empurra todo mundo atingido (`empurrarDe`), alvo principal incluído. `Atravessa Paredes`/`Inesquivável` (`props.ignoraCobertura`) zeram a cobertura de qualquer alvo, principal ou splash. O atirador nunca é pego pelo próprio estouro (excluído da lista de possíveis epicentros/alvos), escolha deliberada pra não haver dano-próprio por acidente de UI.
  - **`Descarregar`** (`props.descarrega`): novo checkbox `🔫 Descarregar` ao lado de `🥷 Furtivo` em `telaMesa`, só aparece se algum ataque equipado tiver essa palavra-chave. Marcado: no lugar do custo normal de munição, consome TODOS os tiros restantes do pente e soma um d(face do dado da arma) extra por bala gasta além do custo normal (`dadosExtraDescarregar`, somado aos dados de dano antes do crítico).
  - **Cuidado ao ler `cat._efeitos` (mods de bancada) vs `pr.efeitos` (palavras-chave)**: são dois caminhos DISJUNTOS de propósito — mods contribuem só via leitura direta de `cat._efeitos` em `[data-atq]` (dano-sem-`contra` somado em `modKw`, acerto somado inline em `mod`), palavras-chave contribuem só via `chavesDeArmasEquipadas`/`k.efeitos.modificarAtaque()` (que lê a arma BASE do `ARMAS`, sem ver mods instalados). Um efeito NUNCA é contado nos dois caminhos ao mesmo tempo — mas se for adicionar um novo tipo de efeito lido em algum desses lugares, checar os dois pra não abrir brecha de dobrar bônus. `armaMontada()` já deduplica automaticamente palavra-chave IDÊNTICA vinda de arma+mod (`Set` sobre a string combinada de `kw`), então instalar um mod com a mesma palavra-chave que a arma já tem não soma nada extra.
  - Toda arma em `ARMAS` e todo mod em `MODS_ARMA` foram conferidos um a um contra o novo `PALAVRAS_CHAVE` — nenhum tem `kw` que caia em keyword desconhecida, ambígua (fuzzy-match) ou sem efeito algum (ver os testes de sanidade em `tests/dados-jogo.test.js`).
- **Fórmula de dano crítico** (regra da mesa): soma **dados + qualquer bônus primeiro**, SÓ DEPOIS multiplica o total por 2 (ou ×4 nos casos que dobram crítico, ex. Assassino veterano furtivo+crítico — `modAtq.multDano` no `[data-atq]`). **Nunca** dobrar a quantidade de dados rolados — isso deixa o bônus fixo de fora da conta e sub-contabiliza o dano. `[data-atq]` (ataque de arma do jogador) sempre fez certo (`(somaDados + danoMod) * multCrit`). Já foi bug real nos outros 3 lugares que resolvem ataque com d20 + dano: `.cb-atk` (ataques derivados/de criatura), disparo de nave inimiga espacial (dois lugares — combate tático embutido e `combate_nave` clássico) — todos rolavam `pd.n * mult` dados em vez de multiplicar a soma final. Os três corrigidos juntos; checar de novo se aparecer mais algum ponto que resolva `d20 + dano` fora de `aplicarEmAlvos`/`aplicarDanoAlvo` (que não têm conceito de crítico).
- **Defesa mostrada no log de ataque**: sempre por extenso, base → modificadores → efetiva (`Def 15 −2🗡 = 13`), nunca só o número final seguido de um modificador solto (`Def 13 · 🗡−2` lia como se ainda faltasse subtrair algo — o 🗡 já estava embutido no 13).
- **Mods de arma** (bancada): `item.mods` = `{ slot: nomeDaPeça }`. `armaMontada(catBase, item)` devolve a arma com `kw`, `_efeitos`, `_tirosExtra`, `_mods` aplicados. Peças em `MODS_ARMA`.
- **Ataque de arma mirado** (`[data-atq]` em `telaMesa`): quando há combate ativo, o seletor de alvo (combatentes do rastreador) só aparece se `souMestre` **ou** for a vez do personagem (`camp.combate.ordem[turno].personagem_id === meuPers.id`); ao mirar, acerto (`total` vs `alvo.cd − pr.ignoraArmadura`) e dano se resolvem sozinhos e o payload da rolagem ganha `alvo_resolvido` (esconde o botão “🩸 aplicar” do Mestre). No acerto, `aplicarCondsNoAlvo()` aplica as condições declaradas: `pr.aoAcertar` (palavra-chave) + `munCond` (munição especial), rolando `d20 + Con` vs o `cd` de cada uma quando há CD. `pr.ignoraArmadura` também vai no payload (`ignoraArmadura`) para o `.m-aplicar` usar quando não houve alvo. `ataquesDoCombatente(c)` deriva ataques de linhas de jogador no rastreador para o Mestre rolar/direcionar por eles.
- **Botão “🩸 aplicar N de dano”** (`.m-aplicar`, Mestre, mensagens de `rolagem` com `dano_total`): se a rolagem carrega `payload.total`, compara com a Defesa efetiva (`alvo.cd − payload.ignoraArmadura`, ou `10 + manobra` para nave). Rolagem < Defesa (ou `fumble`, e sem `crit`) → `confirmModal` de override; recusando, posta “errou” e não aplica. Aplica via `aplicarDanoAlvo`. Ataques sem rolagem de acerto (`payload.total == null`) aplicam direto.
- **`resolve:{tipo:"disputa"}`** (Invasão da Sombra): disputa real de `d20 + atributo` contra um combatente escolhido do rastreador — personagem usa o atributo real (`calc`), inimigo usa `NIVEIS_AMEACA[ameaca].ordem` (0–6) ou `cd − 10`. **Exige alvo**: sem combate ativo / sem candidatos → `alert` e não rola; fechar o modal sem escolher também aborta. Vitória tagueia o alvo com a condição `dominado` (1 turno); derrota causa `derrota.danoProprio`. Marca `usos` (1×/descanso) só se chegou a rolar.
- **`aplicarEmAlvos({...})`** (helper em `telaMesa`, antes de `render`): resolve uma ação contra 1 ou vários combatentes do rastreador. Modos: `acerto` (conjurador rola `d20 + acerto` vs `alvo.cd`), `atributo`+`cd` (alvo faz teste de resistência `d20 + atributo` vs CD — bônus do inimigo = ordem da ameaça ou `cd − 10`), ou nenhum (área que pega todo mundo marcado). `area:true` abre um `select` por combatente (inimigos pré-marcados) para marcar quem está no efeito. Aplica `dado` de dano (rolado 1×) e/ou `cond` por `turnos` a quem o efeito pegou; `snapshot` + `salvarCombate` + resumo em `sistema`.
- **`resolve:{tipo:"salvaguarda", atributo, cd?, dado?, cond?, turnos?, area?}`**: Fogo de Supressão (Sab → Amedrontado), Grito de Saqueador (Sab, área → Amedrontado), Sinfonia do Inverno (Con, área, 4d8 → Paralisado). CD do teste = `dcSalvaguarda(k, atributo)` = 8 + mod.
- **Scripts ofensivos** (`SCRIPTS[i].resolve = {tipo:"ataque", dado, area?}`): Choque Estático, Curto-Circuito em Armadura, Formatar Mente Quântica (alvo único, `d20 + k.conj` vs Def), Sobrecarga de Sistema/Reator (área). O `#conjurar` roteia por `aplicarEmAlvos`.
- **Consumíveis `efeito:"condicao"`** (granadas): roteados por `aplicarEmAlvos` com `area:true`; save só quando o item tem `cd` (usa Con). Sem `cd` → aplica a quem for marcado.
- **Ataques múltiplos de nave**: `NAVES`/naves de admin podem ter `ataques: [{n, bonus?, dano, extra?}]` além do `dano` padrão (os "Canhões"). `ataqueDaNave(nave, origemTxt?)` (módulo-level em `app.js`, perto de `danoNave`) monta o pool `[Canhões, ...ataques]` e só pergunta qual usar (`modalForm`) quando há mais de uma opção — nave só com canhão não muda em nada. `bonus` de cada ataque, se omitido, cai no +4 padrão. Usado nos 3 lugares que resolvem disparo de nave: `[data-cb-nave]` (rastreador tático), `[data-cbn-atk]` (combate_nave clássico) e o posto de Artilharia "Fogo Concentrado"/"Tiro de Precisão" (aí só decide o **dano**, porque o acerto já é o teste de perícia do posto). Editor em `admin.js`: campo tipo `"ataques"` na `ESQUEMA.nave`, mesmo padrão de lista do editor de ataques de criatura (`data-natk`/`data-natk-del`, não confundir com `data-atk`/`data-atk-del` da criatura — telas diferentes, sem colisão real, só nomeado diferente por clareza).
- **Armadura anticrítica**: `efeitos.js` ganhou o tipo `sem_critico` (`momento:"ao_sofrer"`, mesma família do `reducao_dano` do Endurecer) — `k.efeitos.aoSofrer().semCritico`. "Blindagem Dispersora de Impacto" (ARMADURAS, pesada, CD+3, −3 Furtividade, 900 CG) é a primeira a usar. Checado em `[data-atq]` e `.cb-atk`, nos dois SÓ quando o alvo é personagem (`alvo.personagem_id`): calcula o `k` fresco do alvo a partir da cópia em memória de `pers` (não busca no banco — só precisa saber SE está equipada) e força `critAuto`/`critAuto2` a `false`, mesmo com natural 20 ou Paralisado ≤2m. Só anula o ×2/×4 do crítico em si — outro multiplicador (furtivo do Assassino) não é "é crítico" por definição, continua valendo. O log avisa "🛡 armadura anticrítica bloqueia o crítico" em vez de só omitir o marcador — sem isso pareceria que o app errou a conta.
- **Postos de nave** (`[data-est]`): ações com `rola:null` (Fuga de Dobra, Sobrecarga de Propulsores) são tratadas **antes** do `return` genérico — antes elas nunca executavam. *Sobrecarga* → `nt.manobraExtra/manobraAte` (entra em `defesaNaveParty()`, expira na virada de rodada) + dano de choque. *Fuga de Dobra* → `nt.dobra {cargas, cascoRef}`; 2 cargas sem perder casco ⇒ `naveEmCena = false` e a tripulação escapa. *Guerra Eletrônica* → `d20 + Int + Tecnomancia` vs `defesaNave(alvo)`; sucesso põe `alvo.escudosOff = 2` e `danoNave` deixa de absorver com escudo (decrementa no turno da nave; a linha mostra `⚡off`). *Tiro de Precisão* → rola com Desvantagem e, com dano no casco, aplica a condição `Subsistema off` por 1d4 turnos.
- **Scripts de cura/reparo**: `resolve:{tipo:"cura",dado,attr}` (Rollback Celular) e `resolve:{tipo:"reparo_nave",dado}` (Reparo Estrutural em Massa) mandam; o regex no texto (`/Cura (\d+d\d+)/`, `/(\d+d\d+) do casco/`) ficou só como rede para conteúdo do painel admin que ainda não declara `resolve`.
- **PV Temporário e Escudo — mecânicas DIFERENTES de propósito**: `f.pvTemp` e `f.escudoGasto` na ficha; `k.escudoMax = armadura.absorve (Escudo de Energia Pessoal = 10) + k.escudoBonus (soma de efeitos escudo_max)`, `k.escudoLivre = escudoMax − escudoGasto`. Ordem de absorção em `aplicarDanoAlvo`: imunidade a tipo → couraça por limiar → resistência adaptativa → redução (só dano físico) → **Escudo** → **PV Temporário** → corpo. A diferença entre os dois (pedida explicitamente, não é só nomenclatura): **Escudo não deixa passar excedente** — enquanto `escudoLivre > 0` no início da instância de dano, a instância INTEIRA é bloqueada, mesmo que estoure e zere o escudo (`escudo tem 10, toma 15 → os 5 de excesso NÃO chegam no corpo`; só a PRÓXIMA instância, já sem escudo, volta a valer normal). **PV Temporário deixa passar** — é só um estoque a mais de PV, o excedente segue pro corpo depois de esgotá-lo. Qualquer descanso recarrega o escudo e dissolve o PV Temporário. PV Temporário concedido por `resolve:{tipo:"pvtemp",dado,attr?,pericia?,proprio?}` (Reparo Tático, Firewall Ativo) e `resolve:{tipo:"drenar_escudos"}`, não soma (fica o maior). Escudo hoje só vem de `armadura.absorve` (Escudo de Energia Pessoal) ou de um efeito declarado `{tipo:"escudo_max",valor}` em qualquer Portador (implante/filosofia/modo/item) — capacidade genérica, sem conteúdo próprio ainda além da armadura.
- **PV máximo extra**: `f.pvMax` continua sendo o valor BRUTO salvo na ficha (setado manualmente no level-up, em `telaFicha`) — `calc()` nunca mexe nele. O que mudou: `k.pvMax = f.pvMax + k.pvMaxBonus`, onde `k.pvMaxBonus` é a soma de qualquer efeito declarado `{tipo:"pv_max",valor}` (implante/item/modo — ex. "Coração Sintético de Duplo Fluxo", +5). `k.pvMax` é o teto EFETIVO em jogo — todo clamp/exibição de PV (descanso, dano, cura, cura reflexa, ficha, cards, rastreador, painel do Mestre) foi migrado de `f.pvMax` pra `k.pvMax`/`calc(f).pvMax`; só o campo de edição do level-up (`#pvMax` em `telaFicha`) e o gate "ainda não rolou PV" continuam lendo o bruto `f.pvMax`, de propósito. Removido o implante/item que concede isso, o bônus some no próximo `calc()` (o PV atual não é reduzido à força — só passa a clampar contra o teto novo, menor, no próximo dano/cura).
- **Resistência (mais fraca que imunidade)**: `{tipo:"resistencia", a:"<tipo de dano ou nome de condição>"}` em `efeitos.js` (mesma sentinela `"__outro__"` da imunidade — ver `opcoesImuneA` em `admin.js`). Não anula nada: `FichaEfeitos.resistencias()` devolve a lista normalizada em minúsculas, e os dois pontos que já rolam teste de resistência (`aplicarEmAlvos`, no ramo `atributo/pericia + cd`, e `aplicarCondsNoAlvo` de `[data-atq]`, palavra-chave/munição com `cd`) checam `temResistencia(kAlvo, cond) || temResistencia(kAlvo, tipoDano)` e, se bater, rolam o d20 do teste com Vantagem (`Math.max(d(20),d(20))`) em vez de um dado só. Só personagens (tem `k.efeitos`); criaturas não têm equivalente ainda. "Coração Sintético de Duplo Fluxo" é o primeiro caso real: resistência a "Envenenado". Escopo intencional: só vale pra ROLAGENS de resistência/salvaguarda contra aquele dano/condição — não reduz dano já certo de acontecer (isso é `reducao_dano`) nem dá vantagem em ataques.
- **Implantes com ataque próprio (dano em dado)**: um `IMPLANTES[i].ataque = {dano, tipo?, per?, attr?, kw?, efeitos?}` (mesmo shape de uma entrada de `ARMAS`) faz o implante aparecer como mais uma opção em `armasEq`/`[data-atq]` em `telaMesa`, sempre "equipado" enquanto instalado (não passa por `f.inventario`). `implanteComoArma(nome)` (app.js, perto de `render`) traduz o implante num objeto tipo-arma; `catDoAtaque(nome) = todasArmas().find(...) || implanteComoArma(nome)` substitui os lookups antigos que só olhavam `todasArmas()` (no botão de ataque e no handler `[data-atq]`). Ícone ⧉ em vez de ⚔ distingue visualmente. Como o implante não está em `f.inventario`, seu `kw` NÃO entra em `k.efeitos` via `chavesDeArmasEquipadas` (isso só olha armas equipadas de verdade) — qualquer efeito que o ataque do implante precise (ex. dano dobrado em furtivo) deve ir no `efeitos` do PRÓPRIO implante (fonte já contada em `FichaEfeitos.de()` independente de ataque), não dentro de `ataque.kw`. "Lâmina Oculta Retrátil (Mantis)" é o primeiro caso: `ataque:{dano:"1d8",tipo:"branca",kw:"Oculta"}` + `efeitos:[{tipo:"multiplicar_dano",fator:2,quando:"desprevenido",momento:"ao_atacar"}]` (mesmo efeito que o Assassino já usa pra furtivo — sem filtro por arma, é uma imprecisão aceita, mesmo padrão do Braço Mecânico Hidráulico).
- **Cortex Central de Nano-Enxame** (`IMPLANTES`, antes chamado "Tatuagens de Nano-Enxame") — o único implante da família; libera a compra das formas `ARMAS[i].nano === true` ("Nano-Tatuagem: X", 10 opções hoje — 8 originais + `Lâmina-Guarda de Enxame`/kw `Aparar` e `Estilete Perfurante de Enxame`/kw `Perfurante`, adicionadas pra cobrir keywords que as outras 8 não usavam). Gate em `telaFicha`: `(f.implantes||[]).includes("Cortex Central de Nano-Enxame")` mostra a seção; cada forma tatuada é um item normal de `f.inventario` (`tipo:"arma"`, preço de tabela 0 — o custo real é cobrado à parte). **Limite por porte, não mais "3 grátis"**: `RACAS[i].tamanho` (`"pequena"|"média"|"grande"`, novo campo — classificado pelo `dadoVida` da raça: d6→pequena, d8→média, d10→grande) indexa `LIMITE_TATUAGENS` (`dados-jogo.js`: `{pequena:3, média:5, grande:8}`). Toda forma tatuada custa 250 CG (não tem mais grátis); até o limite do porte, não ocupa slot de implante — a partir da forma seguinte, CADA forma extra também ocupa **1 slot do Limite Cibernético** (`k.limite`, `regras.js`). Implementado sem infraestrutura genérica de pré-requisito de implante (decisão explícita do usuário: "específico só para nano-enxame") — o consumo de slot é só o próprio nome da forma (`"Nano-Tatuagem: X"`) empurrado pra dentro de `f.implantes` quando comprada além do limite, e removido de lá ao dissolver essa forma. Funciona de graça com todo o resto do app porque `f.implantes.length` já é a contagem usada em `k.limite`/carga do Cinético (`regras.js`) e em `FichaEfeitos.de()` (`efeitos.js`) — que faz `IMPLANTES.find(x => x.n === nome)` e simplesmente ignora nomes que não batem com nenhum implante de catálogo, então o marcador de slot não aplica nenhum efeito de implante fantasma, só ocupa espaço na contagem. Ao tatuar/dissolver, `[data-nano]` em `telaFicha` recalcula `precisaSlot` e `slotLivre` a cada clique (não confiar em estado cacheado entre handlers).
- **Furtividade real** (antes 100% autodeclarada — o checkbox 🥷 só existia como `$("#atq-furtivo")?.checked`, sem checagem nenhuma por trás): em `[data-atq]`, `furtivo` agora só fica `true` se `furtivoMarcado` **e** o alvo realmente está desprevenido (`condsAlvo.includes("surpreso")` OU `alvoAindaNaoAgiu` — 1ª rodada e a posição do alvo na ordem ainda não chegou no turno atual). Sem alvo do rastreador (fora de combate), o checkbox continua valendo sozinho — o Mestre adjudica. Quando o jogador marca mas não qualifica, `furtivoNegado` aparece nos marcadores da rolagem ("🥷 furtivo NÃO vale — alvo não está desprevenido") em vez de falhar silenciosamente. Limitação conhecida: o checkbox em si não é desabilitado na UI (ele é renderizado ANTES do alvo ser escolhido, no `modalForm` de mira dentro do próprio handler) — a validação é só na hora de resolver o ataque. Não criado: um estado real de "detectado/oculto" por combatente (ficou de fora, era subsistema novo do zero).
- **Recompensas de missão/contrato**: `camp.contratos[i]` ganhou `recompensaCg`, `recompensaXp` (numéricos, opcionais, preenchidos na criação) e `recompensaItens: [{tipo, nome, qtd}]` (arma/armadura/implante/item/nave, adicionados um a um pelo botão "➕ Item" no card do contrato — mesmo seletor de categoria+nome do "🎁 Conceder equipamento"). O botão "🎁 Entregar" no card dispara tudo de uma vez: CG/XP pelo mesmo canal `enviar("recompensa",...)` que `mestre-xp`/`mestre-cg` usam (cada cliente conectado aplica na própria ficha ao ouvir a mensagem, realtime), cada item via `payload.loot` (igual `mestre-loot`). Nave é especial — não há inventário de naves por jogador: sem `camp.nave`, vira a nave da party direto; com `camp.nave` já existente, pede confirmação pra SUBSTITUIR (só existe uma nave de party no app hoje).
- **Tipos de dano**: `TIPOS_DANO` (físico, térmico, químico, elétrico, psíquico, gélido, ácido, verdadeiro). `tipoDanoArma(cat)` deriva da palavra-chave; `tipoDanoAtaque(atk)` lê o `extra` da criatura; munição especial sobrepõe (`TIPOS_PENTE[x].tipoDano`); scripts/habilidades/granadas declaram `tipoDano`. `imuneAoDano(habs, tipo)` respeita `imunidade` cujo `a` é um tipo (Espectro do Vácuo = imune a físico). "verdadeiro" ignora tudo.
- **Economia de ações**: `linha.acoes = {p, m, r}` (gastas) na linha do rastreador. `gastarAcao(textoDaAcao, oque)` mapeia "Ação Principal"→`p`, "Ação de Movimento"→`m`, "Reação"→`r`, Ação Livre→grátis; bloqueia o jogador e deixa o Mestre forçar. `p`/`m` zeram no turno do próprio; `r` zera na virada de rodada — por isso **Reações funcionam fora do turno**. Consomem: atacar, conjurar (pelo `s.a`), habilidade (pelo `hRaw.acao`), item (`cfg.acao`), mover no campo e trocar pente. Indicador `P M R` na linha de quem está agindo.
- **Cobertura**: `c.cobertura` 0/1/2 (botão 🚫/🧱/🏚 na linha, Mestre). Soma `+0/+2/+5` na Defesa **só contra ataques à distância** (arma branca ou `alcance ≤ 3 m` ignora).
- **Surpresa / eventos**: `#cb-surpresa` marca quem emboscou; o resto ganha a condição `Surpreso` (1 turno, bloqueia ação) — Código do Sobrevivente e imunidades com /surpres/ passam. `#cb-evento` agenda `camp.combate.eventos[]` com contagem regressiva por rodada.
- **Bandos**: linha do rastreador com `qtd > 1` representa N lacaios. O dano transborda: mata um, o excesso vai para o próximo. Mostra `×N` no nome e no token.
- **1×/sessão**: `abilidadesDeSessao(f)` cataloga `freq:"sessao"` (ou texto "1x/sessão"); o Mestre abre uma sessão pelo painel (`mestre-sessao` → `descanso` com `payload.tipo="sessao"`), e cada cliente limpa os `usos` correspondentes.
- **Overclock de RAM**: sem RAM, `#conjurar` oferece pagar com Vida — Bateria Interna (1d8, scripts de custo ≤2) ou Overclock manual (1d6 por ponto que falta).
- **Macros de rolagem** (`#dado-livre`): `macrosDe(pid)`/`salvarMacros(pid, lista)` guardam até 12 expressões por personagem em `localStorage["ps-macros-"+pid]` — só no navegador, não sincroniza entre dispositivos nem tem coluna no banco (é preferência de UI, não estado de campanha). `☆` salva a expressão do campo como macro (pede um rótulo curto); cada macro vira um par de botões (rolar · `✕` remover). `rolarLivre(v)` foi extraído do handler de `#rolar-livre` pra ser reusado pelos chips.
- **Teste oposto** (`#teste-oposto`): escolhe a sua perícia, o alvo e a perícia dele; rola os dois e o empate favorece a defesa.
- **Scripts/habilidades mecanizados por `resolve`** (além dos ofensivos e das salvaguardas já listados): **Cegueira Cibernética** `{tipo:"ataque",cond:"Cego",turnos:2}` (sem `dado` — puro controle; o branch `tipo:"ataque"` do `#conjurar` repassa `cond`/`turnos` pra `aplicarEmAlvos`, então script ofensivo pode ser dano, condição ou os dois). **Hackear Implante Motor** `{tipo:"ataque",cond:"Motor Travado",turnos:3}`. **Repulsão Cinética** `{tipo:"salvaguarda",atributo:"For",cond:"Caído",turnos:2,area:true,raio:2,empurrao:3}` — área de 2 m por ponto de impacto, empurra 3 m e derruba quem falhar em Força. **"Espere, podemos resolver isto"** `{tipo:"salvaguarda",atributo:"Sab",cond:"Hesitante",turnos:1}`.
- **`efeito:{tipo:"adaptar", valor}`** (criaturas, `gatilho:"ao_sofrer"`) — Evolução Reativa do Enxame Adaptativo. Em `aplicarDanoAlvo`: se `alvo.resist[tipo]` já existe, abate esse valor do dano; senão, e se a criatura tem a habilidade, grava `resist[tipo] = valor` em **todas as linhas do mesmo bicho** no rastreador (comparando o nome sem o sufixo ` #N`) — "todo o Enxame ganha resistência". Dano `verdadeiro` nunca é resistido nem ensina nada.
- **Ainda narrativas** (mensagem de sistema, Mestre adjudica): scripts de controle sem `resolve` — Glitch Visual, Travar Armamento, Trava Biométrica, Desativar Suporte de Vida, Loop de Feedback, Hackear Navegação Veicular.

## Supabase

**Tabelas**: `perfis` (id, apelido, avatar_url), `personagens` (dono_id, campanha_id, nome, `dados` JSONB, publico, token_publico, atualizado_em), `campanhas` (nome, codigo, mestre_id + colunas JSONB `nave`/`mapa`/`combate`/`bestiario`/`contratos`/`faccoes`/`handout`), `campanha_membros` (campanha_id, perfil_id, posto), `mensagens` (campanha_id, autor_id, personagem_id, tipo, conteudo, `payload` JSONB), `mestre_notas` (campanha_id, texto), `conteudo` (tipo, chave, `dados` JSONB — conteúdo global do painel admin).

**Mensagens** `tipo`: `mensagem` | `rolagem` | `sistema` | `descanso`. Rolagens guardam `payload.total`, `payload.dano_total`, `crit`, `fumble`, etc.

**RPC**: `entrar_campanha({ cod })` → id da campanha.

**Realtime**: `sb.channel('mesa-<id>')` ouve INSERT em `mensagens`. Persistência de estado de combate/nave é o Mestre gravando colunas JSONB de `campanhas` (com `snapshot()` para desfazer).

**Cuidado — histórico do chat**: toda busca de `mensagens` com `limit` **sem** filtro de "a partir de agora" precisa ser `.order("criado_em", { ascending: false }).limit(N)` e **depois inverter o array** (`[...data].reverse()`) antes de usar em ordem cronológica. Buscar `ascending: true` + `limit(N)` pega as N mensagens mais **antigas**, não as mais recentes — numa campanha com mais de N mensagens (fácil de bater: cada ação de combate gera várias `sistema`), tudo recente some, sem erro nenhum. Já foi bug real (histórico do chat em `telaMesa`, backup em `fazerBackup`, linha do tempo em `painelLin`, `#abrir-diario` — os quatro corrigidos juntos). Exceção correta: `recapitular()` usa `.gt("criado_em", ultima).order(asc)` — aí `ascending: true` está certo, porque o corte já é "desde a última visita" pra frente.

**Cuidado — salvamento sem checar erro**: toda gravação em `campanhas` passa por `salvarCamp(campos, rotulo)` (em `telaMesa`) e toda gravação de `personagens.dados` por `salvarFicha(personagemId, dados, rotulo)` (módulo-level, usado por `telaFicha` e `telaMesa`) — os dois checam `error` e mostram `alert` sozinhos, devolvendo `true`/`false`. **Nunca** chamar `sb.from("campanhas").update(...)` ou `sb.from("personagens").update({ dados })` direto de novo — já foi auditoria real: mais de 55 chamadas assim (posto de nave, dano/cura, RAM, level up, descanso, recompensa, reparo de nave…) salvavam sem checar erro nenhum. Numa falha de rede/RLS o app já tinha renderizado como se tivesse dado certo, e a mudança sumia sem aviso na sincronização seguinte — a mesma categoria do bug do histórico do chat, só que na escrita em vez da leitura. `salvarFicha` tem rótulo default `"salvar a ficha"`, então é seguro chamar só com `(id, dados)` quando não há uma frase melhor.

**Cuidado — handlers "mutar tudo, salvar 1×"**: vários handlers grandes (`#cb-prox`, `[data-atq]`, `.cb-atk`) mutam `camp.combate` em vários pontos e só chamam `salvarCombate()`/`salvarCampanha()` **uma vez no final**, pra não disparar o eco do realtime no meio. Qualquer exceção lançada entre o início do handler e esse save final faz a rodada inteira (dano, condição, turno, invocação) ficar só na tela e sumir ao recarregar — sem alerta nenhum, porque o `catch` de erro do Supabase nunca é alcançado. Blocos de efeito automático/narrativo (`habsDoCombatente(...).disparar(...)`, gatilhos de criatura) devem ir em `try/catch` isolado para não arriscar o save principal.

**Storage**: upload de foto de personagem (comprimida no cliente antes do envio).

## Convenções

- Vanilla JS, sem framework. Telas = funções que geram string HTML e fazem `app.innerHTML = ...`, depois religam handlers (`app.querySelectorAll(...).forEach(b => b.onclick = ...)`). Re-render total, não diffing.
- `$` / `$$` = atalhos de `querySelector`/`All`. `esc()` sempre em texto vindo do usuário dentro de template string.
- Confirmações: `confirmModal` / `modalForm` de `ui.js` — nunca `alert`/`confirm` nativos para fluxo (só `alert` para erro seco).
- Ações destrutivas do Mestre usam `avisar(texto, aoDesfazer)` (desfazer) ou `snapshot()` antes.
- Sem dependências novas sem necessidade real. Sem passo de build — o que está no arquivo é o que roda.
- PT-BR em identificadores de domínio (`arma`, `pente`, `nave`, `posto`, `mestre`).

## Touch/mobile — "só aparece no :hover" é bug, não estilo

Um elemento com `opacity:0` que só vira visível em `:hover` **não existe** em celular/tablet — não há `:hover` sem mouse, e o elemento fica lá, funcional, mas impossível de descobrir. Já foi bug real duas vezes: `.card-del` (apagar personagem no Hangar) e `.m-reply` (responder mensagem no chat) ficavam em `opacity:0` de repouso. Corrigido pra um valor visível-mas-discreto em repouso (`.4`–`.55`) que intensifica no hover — nunca `0`. Mesmo raciocínio vale pro campo tático: o "modo expandido" (pistas mais altas, tokens maiores, grade de 1 m) era só `:hover`; agora `.cb-campo.perto` (ligado clicando `#cb-zoom`, funciona em qualquer dispositivo) dispara o mesmo CSS. Ao criar um controle secundário "que só aparece ao passar o mouse", sempre perguntar: como alguém sem mouse ativa isso?

O mesmo raciocínio vale pra "como alguém sem teclado FECHA isso": o painel de acessibilidade (`montarA11y()` em app.js) só tinha `Esc` (não existe em celular) e reclicar no próprio ♿ (o mesmo ícone de abrir, sem nenhuma pista visual de que também fecha) — no toque, quem abria ficava preso, sem jeito de sair sem recarregar a página. Corrigido com um padrão de modal completo: `.a11y-fundo` (overlay escurecido, `position:fixed;inset:0`, fecha ao tocar fora) atrás do painel, e um botão `✕` (`.a11y-x`) visível no canto superior direito — os três fecham pelo mesmo `fecharPainel()`, então nenhum caminho de fechar pode ficar sem os outros dois. Terceira instância do mesmo problema: descobrir "como fecho isto sem mouse/teclado" é a pergunta certa pra QUALQUER painel/overlay novo, não só os dois primeiros casos.

**Tela cheia de verdade, em vez de descontar o padding do body na conta**: `body` tem `padding:20px 14px 60px` fixo (topo do CSS) — bom pra páginas de conteúdo normais, ruim pra uma tela que precisa centralizar contra a altura EXATA do viewport (login). A primeira tentativa foi um `min-height:calc(100vh - 80px)` descontando os 80px de padding na mão — funcionava, mas ficava acoplado a um número que quebraria silenciosamente se o padding do body mudasse por qualquer outro motivo. Solução mais limpa: `shell(titulo, ...)` (app.js) liga `body.pagina-cheia` quando `titulo === "login"`; `body.pagina-cheia{padding:0}` zera o padding só nessa tela, e `.tela-login` vira um container de tela cheia de verdade — `min-height:100vh` (com `100dvh` por cima pra acompanhar a altura visível real no celular, onde a barra de endereço muda `100vh` dinamicamente) e o próprio padding lateral (`20px 14px`), sem depender de nenhum valor emprestado do body. Qualquer tela nova que precise ocupar "a tela inteira" centralizada segue o mesmo padrão: uma classe no body pra zerar o padding herdado, não uma conta de `calc()` acoplada a um número que pode mudar.

## Camadas de z-index (CSS) — fonte recorrente de bug

`.mp-overlay` 9998 · `.ss-overlay` 9999 · `.pular-link` 10001 · **bancada** (inline em `abrirBancada`) 10020 · `.cri-overlay` 10030 · `.aviso` 10040 · `.nv-cena` 10050 · **`.mdl-overlay`** (modais de `ui.js`) **10060** — deve ficar acima de tudo, pois exige decisão do usuário. Ao criar um overlay novo, conferir se um `confirmModal` disparado de dentro dele ainda aparece por cima.

## Deploy (Vercel)

Root Directory vazio · Output Directory `public` · sem Build/Install Command. Após mudança relevante em arquivo do `SHELL`, **incrementar `CACHE` em `public/sw.js`** senão o PWA serve a versão velha.

## Testes automatizados

`tests/*.test.js`, testador embutido do Node (`node:test` + `node:assert/strict`) — zero instalação, zero `package.json`/`node_modules` novo, roda com:

```
node --test
```

**Sem caminho, de propósito**: o Node varre o projeto e roda tudo que casa com `*.test.js`. Não usar `node --test tests/` — a partir do Node 22 um diretório solto é tratado como arquivo e o runner morre com `Cannot find module '.../tests'`.

**Sem Node instalado na máquina de quem desenvolve** (não pode/não quer instalar): `.github/workflows/testes.yml` roda os mesmos testes automaticamente a cada `git push`/Pull Request, na nuvem do GitHub — zero instalação local. Resultado aparece na aba **Actions** do repositório no GitHub, e como ✓/✗ ao lado do commit. Esse é o jeito recomendado de rodar os testes quando não há Node local.

Três arquivos hoje:
- `tests/dados-jogo.test.js` — `propsArma`/`chavesDaArma`, sanidade de conteúdo que percorre `ARMAS` inteiro conferindo que toda palavra-chave cadastrada é reconhecida (pegou a própria regressão do fix de "texto duplicado nas palavras-chave"), e o mesmo tipo de sanidade para `IMPLANTES` com `.ataque` declarado (dado parseia, `kw` reconhecido).
- `tests/regras.test.js` — `calc` (ficha derivada), `dcSalvaguarda`, `aplicarCond` (empilhar dano vs. renovar estado, `origemId`), `distCombate`/`posInicial`, tipos de dano/alcance, `parseDice`, `danoCritico` (a fórmula "soma dados+bônus primeiro, multiplica depois"), a armadura anticrítica, e `k.pvMax`/`k.escudoMax` (PV máximo extra e Escudo via efeito declarado, com "Coração Sintético de Duplo Fluxo" como caso real).
- `tests/validacao.test.js` — `validar()` (criaturas.js) e `validarEfeitos()` (efeitos.js) pro select "Imune a"/"Resistente a" + sentinela `"__outro__"`, e testes diretos de `FichaEfeitos`/`Portador` pros efeitos `pv_max`/`escudo_max`/`resistencia` (unitários, sem passar por `calc()`).

**Por que dá pra testar `calc()`/`aplicarCond()`/a fórmula de crítico agora**: elas foram extraídas de `app.js` para `public/js/regras.js`, um módulo sem nenhum import de rede (só `dados-jogo.js` e `efeitos.js`, ambos igualmente limpos). Antes disso não dava: a primeira linha de `app.js` é `import { createClient } from "https://esm.sh/@supabase/supabase-js@2"`, uma URL remota que o resolvedor de módulos padrão do Node não entende, então nada do arquivo era importável em teste. `app.js` continua com o mesmo comportamento — só passou a importar essas funções de volta de `regras.js` em vez de declará-las localmente. Se for extrair mais alguma coisa de lá no futuro: confira sempre se a função usa algo do DOM (`document`), de `sb` (Supabase) ou de estado que só existe dentro de `telaMesa`/`telaFicha` — só migra o que for de fato puro (recebe dado, devolve dado).

## Verificação de mudanças

Não há testes. Para validar:
1. `cd public && python -m http.server 3000`, abrir no navegador com `config.js` apontando p/ um projeto Supabase real.
2. Exercitar o fluxo tocado (ex.: criar personagem → equipar arma → entrar em campanha → atirar no chat → abrir bancada e instalar/remover mod).
3. Olhar o console do navegador — um `ReferenceError` num handler faz o clique "não fazer nada" silenciosamente.
