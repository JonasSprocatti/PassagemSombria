# Chaves locais — gerado no build da Vercel, nunca vai pro repo
public/js/config.js

# Sistema operacional
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Vercel (se você usar `vercel dev` / CLI, ela cria essa pasta com config local)
.vercel

# Node (se um dia adicionar dependências ou scripts de build)
node_modules/
